#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n; cin >> n;
    for(int mask = 0; mask < (1 << n); mask++)
    {
        for(int i = n-1; i >= 0; i--)
        {
            if(mask & (1 << i)) cout << 1;
            else cout << 0;
        }
        cout << endl;
    }
}
